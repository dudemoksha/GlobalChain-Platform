services:
  - type: web
    name: globalchain-backend
    env: python
    buildCommand: pip install -r globalchain-backend/requirements.txt
    startCommand: uvicorn globalchain-backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000
